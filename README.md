# Local-research-assistant-
Local multi-agent research pipeline (planner → search workers → fact-checking critic) built on Ollama, Tavily, and Exa — with an eval harness and dashboard to prove it's worth the extra cost over a single LLM call.
